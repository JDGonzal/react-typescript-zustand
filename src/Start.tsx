import { Button } from "@mui/material";

function Start() {
  return (
    <div>
      <Button onClick={() => { }} variant="contained">
        ¡Vamos a Empezar!
      </Button>
    </div>

  )
}

export default Start